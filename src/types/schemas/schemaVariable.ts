import { fieldsVariable } from "../fields/fields-variable";

export const schemaVariable = {
  singular: "variable",
  plural: "variables",
  label: "Variável",
  labelPlural: "Variáveis",
  fields: fieldsVariable,
};
