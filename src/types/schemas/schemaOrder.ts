import { fieldsOrder } from "../fields/fields-order";
import { fieldsQuote } from "../fields/fields-quote";

export const schemaOrder = {
  singular: "quote",
  plural: "quotes",
  label: "Pedido",
  labelPlural: "Pedidos",
  fields: fieldsOrder,
};
