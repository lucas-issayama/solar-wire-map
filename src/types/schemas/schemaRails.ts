import { fieldsRail } from "../fields/fields-rail";
import { fieldsSupport } from "../fields/fields-support";

export const schemaRail = {
  singular: "rail",
  plural: "rails",
  label: "Trilho",
  labelPlural: "Trilhos",
  fields: fieldsRail,
};
