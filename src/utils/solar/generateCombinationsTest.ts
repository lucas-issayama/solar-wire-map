// import MpptsSelector from "@/components/ui/mppt-selector";

// export function generateCombinations(mppts: any, n: number) {
//   let result: any = null; // Store the first result
//   //{ total, mppts: [...currentConfig] };

//   let index = 0;
// }

// function getOption(index: number, mppts: number[][], n: number) {
//   let foundExact = mppts[index].find((el) => n);

//   if (foundExact) {
//     return mppts.map((el, i) => (i == 0 ? n : 0));
//   }

//   if (index < mppts.length) {

//     //Maior que o disponível testar usando o maior
//     if (n > mppts[index][mppts[index].length - 1]) {
//       let result: any = getOption(  );
//     }

//     // if(mptts[mptts.length-1][1]>=n)
//     //   return mptts[mptts.length-1]

//     //Search exact

//     //OtherWise test all
//   }else{
//     return false
//   }
// }
